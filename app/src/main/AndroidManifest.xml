<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vivalavida.informeencuentrodeamistad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/new_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/new_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InformeEncuentroDeAmistad">
        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>

        <activity android:name=".SecondActivity"/>
        <activity android:name=".TirdActivity"/>
        <activity android:name=".FourActivity"/>
        <activity android:name=".GetUserDateActivity"/>
        <activity android:name=".GetUserDetailsActivity"/>
        <activity android:name=".PersonAgregaActivity"/>

        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>